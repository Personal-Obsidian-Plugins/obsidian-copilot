/**
 * Minimal set of supported key bindings for accepting autocomplete suggestions.
 * Retained for settings compatibility while the autocomplete feature is disabled.
 */
export type AcceptKeyOption = "Tab" | "Space" | "ArrowRight";
